name: Updates Docs

on: [push]

jobs:
  update-docs-windows:

    runs-on: windows-latest

    steps:
    - name: Run True DAT
      run: .\true-dat\true-documentation-automation-tool.exe .\AsyncExamples\
